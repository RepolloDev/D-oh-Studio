---
import Tag from "../Tag.astro";

const tags = ["Experto", "Atómico...", "D'oh", "Trabajo muy duro"];
---

<section
  class="w-full h-[75dvh] flex flex-col items-center justify-center md:mb-10 lg:mb-20"
>
  <div
    class="donut w-60 h-60 sm:w-72 sm:h-72 md:w-80 md:h-80 xl:w-96 xl:h-96 absolute -z-10 top-0 left-0"
  >
  </div>
  <h1
    class="text-2xl md:text-5xl xl:text-7xl font-bold text-center w-[20ch] mt-40 mb-10 leading-tight"
  >
    Inspector de <font class="text-blue">seguridad nuclear</font>, listo para
    cualquier desafio 🚀
  </h1>
  <div
    class="max-w-[800px] tags-container flex flex-wrap gap-5 font-sans justify-center"
  >
    {
      tags.map((tag) => (
        <Tag>{tag}</Tag>
      ))
    }
  </div>
</section>

<style>
  .donut {
    background-image: url("donut.webp");
    background-position: center;
    background-size: cover;
    animation: rotate 10s linear infinite;
  }

  @keyframes rotate {
    0% {
      transform: translate(-50%, -50%) rotate(0deg);
    }
    100% {
      transform: translate(-50%, -50%) rotate(360deg);
    }
  }
</style>
