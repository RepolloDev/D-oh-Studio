---
import GlassCard from "../GlassCard.astro";
---

<section class="px-5 md:px-10 xl:px-20 md:mb-10 lg:mb-20">
  <div class="grid lg:grid-cols-2 gap-5 md:gap-10">
    <!-- {dataList.map((item, index) => <GlassCard {...item} />)} -->
  </div>
</section>
